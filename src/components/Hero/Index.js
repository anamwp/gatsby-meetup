import React, { Component } from 'react'
import {HeroWrapper} from './Style'


export default class Index extends Component {
    render() {
        return (
            <HeroWrapper>
                <p>Gatsby is a modern website framework that builds performance into every website by leveraging the latest web technologies. Create blazing fast, compelling apps and websites without needing to become a performance expert.</p>
            </HeroWrapper>
        )
    }
}
